{
    "name": "demo",
    "version": "1.0.0",
    "description": "",
    "main": "demo.js",
    "scripts": {
        "dev": "rimraf dev-dist && parcel serve src/index.html -d dev-dist --port 3000",
        "build": "rimraf dist && parcel build src/index.html -d dist"
    },
    "author": "Samrith Shankar",
    "license": "ISC",
    "devDependencies": {
        "@babel/core": "^7.1.6",
        "@babel/plugin-proposal-class-properties": "^7.1.0",
        "@babel/preset-env": "^7.1.6",
        "gh-pages": "^2.0.1",
        "parcel-bundler": "^1.10.3"
    },
    "dependencies": {
        "concurrent-tasks": "^0.0.3"
    }
}
